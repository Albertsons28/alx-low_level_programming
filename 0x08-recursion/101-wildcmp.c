#include<stdio.h>
#include"main.h"
/**
 *  a function that compares two strings and returns 1
 *  if the strings can be considered identica
 *  Return: 1 or 0 otherwise
 */
int compare_strings(char *s1, char *s2)
{
	if ((strcmp(s1, s2) == 0)
			{
			return (1);
			}
			else
			{
			return (0);
			}
}
